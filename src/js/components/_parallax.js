import { $WIN } from '../_constants';

export default (function() {

	const $items = $('[data-parallax]');

	$WIN.on('scroll', () => {

		$items.each(function() {

			const $item = $(this);
			

		});

	});

})();